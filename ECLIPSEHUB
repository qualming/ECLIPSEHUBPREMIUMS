do local TweenService=game:GetService("TweenService");local UserInputService=game:GetService("UserInputService");local Players=game:GetService("Players");local LocalPlayer=Players.LocalPlayer;local StarterGui=game:GetService("StarterGui");local RunService=game:GetService("RunService");if LocalPlayer.PlayerGui:FindFirstChild("EclipseHub") then LocalPlayer.PlayerGui:FindFirstChild("EclipseHub"):Destroy();end local function sendNotification(title,text,duration) pcall(function() StarterGui:SetCore("SendNotification",{Title=title,Text=text,Duration=duration or 5 });end);end task.wait(0.5);sendNotification("Eclipse Hub","Loaded! Discord: discord.gg/3pc4mUYvVs",8);local ScreenGui=Instance.new("ScreenGui");ScreenGui.Name="EclipseHub";ScreenGui.ResetOnSpawn=false;ScreenGui.ZIndexBehavior=Enum.ZIndexBehavior.Sibling;ScreenGui.Enabled=false;ScreenGui.Parent=LocalPlayer.PlayerGui;local guiVisible=true;local ToggleButton=Instance.new("TextButton");ToggleButton.Name="ToggleButton";ToggleButton.Size=UDim2.new(0,60,0,60);ToggleButton.Position=UDim2.new(0,20,0.5, -30);ToggleButton.BackgroundColor3=Color3.fromRGB(255,215,0);ToggleButton.BorderSizePixel=0;ToggleButton.Text="";ToggleButton.ZIndex=1000;ToggleButton.Parent=ScreenGui;local ToggleCorner=Instance.new("UICorner");ToggleCorner.CornerRadius=UDim.new(1,0);ToggleCorner.Parent=ToggleButton;local ToggleGradient=Instance.new("UIGradient");ToggleGradient.Color=ColorSequence.new({ColorSequenceKeypoint.new(0,Color3.fromRGB(255,215,0)),ColorSequenceKeypoint.new(1,Color3.fromRGB(255,165,0))});ToggleGradient.Rotation=45;ToggleGradient.Parent=ToggleButton;local EclipseShadow=Instance.new("Frame");EclipseShadow.Name="EclipseShadow";EclipseShadow.Size=UDim2.new(0,45,0,45);EclipseShadow.Position=UDim2.new(0,10,0,5);EclipseShadow.BackgroundColor3=Color3.fromRGB(20,20,25);EclipseShadow.BorderSizePixel=0;EclipseShadow.ZIndex=1001;EclipseShadow.Parent=ToggleButton;local EclipseShadowCorner=Instance.new("UICorner");EclipseShadowCorner.CornerRadius=UDim.new(1,0);EclipseShadowCorner.Parent=EclipseShadow;local ToggleBorder=Instance.new("UIStroke");ToggleBorder.Color=Color3.fromRGB(255,215,0);ToggleBorder.Thickness=3;ToggleBorder.Parent=ToggleButton;local MainFrame=Instance.new("Frame");MainFrame.Name="MainFrame";MainFrame.Size=UDim2.new(0,550,0,400);MainFrame.Position=UDim2.new(0.5, -275,0.5, -200);MainFrame.BackgroundColor3=Color3.fromRGB(20,20,25);MainFrame.BorderSizePixel=0;MainFrame.Active=true;MainFrame.Parent=ScreenGui;local toggleDragging=false;local toggleDragInput,toggleDragStart,toggleStartPos;local dragDistance=0;local function updateToggle(input) local delta=input.Position-toggleDragStart ;dragDistance=math.sqrt((delta.X^2) + (delta.Y^2) );ToggleButton.Position=UDim2.new(toggleStartPos.X.Scale,toggleStartPos.X.Offset + delta.X ,toggleStartPos.Y.Scale,toggleStartPos.Y.Offset + delta.Y );end ToggleButton.InputBegan:Connect(function(input) if ((input.UserInputType==Enum.UserInputType.MouseButton1) or (input.UserInputType==Enum.UserInputType.Touch)) then toggleDragging=true;toggleDragStart=input.Position;toggleStartPos=ToggleButton.Position;dragDistance=0;input.Changed:Connect(function() if (input.UserInputState==Enum.UserInputState.End) then toggleDragging=false;if (dragDistance<10) then guiVisible= not guiVisible;if guiVisible then MainFrame.Visible=true;TweenService:Create(MainFrame,TweenInfo.new(0.3,Enum.EasingStyle.Back,Enum.EasingDirection.Out),{Size=UDim2.new(0,550,0,400)}):Play();else local hideTween=TweenService:Create(MainFrame,TweenInfo.new(0.3,Enum.EasingStyle.Back,Enum.EasingDirection.In),{Size=UDim2.new(0,0,0,0)});hideTween:Play();hideTween.Completed:Connect(function() MainFrame.Visible=false;end);end end end end);end end);ToggleButton.InputChanged:Connect(function(input) if ((input.UserInputType==Enum.UserInputType.MouseMovement) or (input.UserInputType==Enum.UserInputType.Touch)) then toggleDragInput=input;end end);UserInputService.InputChanged:Connect(function(input) if ((input==toggleDragInput) and toggleDragging) then updateToggle(input);end end);local dragging=false;local dragInput,dragStart,startPos;local function update(input) local delta=input.Position-dragStart ;MainFrame.Position=UDim2.new(startPos.X.Scale,startPos.X.Offset + delta.X ,startPos.Y.Scale,startPos.Y.Offset + delta.Y );end MainFrame.InputBegan:Connect(function(input) if ((input.UserInputType==Enum.UserInputType.MouseButton1) or (input.UserInputType==Enum.UserInputType.Touch)) then dragging=true;dragStart=input.Position;startPos=MainFrame.Position;input.Changed:Connect(function() if (input.UserInputState==Enum.UserInputState.End) then dragging=false;end end);end end);MainFrame.InputChanged:Connect(function(input) if ((input.UserInputType==Enum.UserInputType.MouseMovement) or (input.UserInputType==Enum.UserInputType.Touch)) then dragInput=input;end end);UserInputService.InputChanged:Connect(function(input) if ((input==dragInput) and dragging) then update(input);end end);local MainCorner=Instance.new("UICorner");MainCorner.CornerRadius=UDim.new(0,12);MainCorner.Parent=MainFrame;local BorderFrame=Instance.new("Frame");BorderFrame.Name="BorderFrame";BorderFrame.Size=UDim2.new(1,4,1,4);BorderFrame.Position=UDim2.new(0, -2,0, -2);BorderFrame.BackgroundColor3=Color3.fromRGB(255,215,0);BorderFrame.BorderSizePixel=0;BorderFrame.ZIndex=0;BorderFrame.Parent=MainFrame;local BorderCorner=Instance.new("UICorner");BorderCorner.CornerRadius=UDim.new(0,12);BorderCorner.Parent=BorderFrame;local BorderGradient=Instance.new("UIGradient");BorderGradient.Color=ColorSequence.new({ColorSequenceKeypoint.new(0,Color3.fromRGB(255,215,0)),ColorSequenceKeypoint.new(0.5,Color3.fromRGB(255,165,0)),ColorSequenceKeypoint.new(1,Color3.fromRGB(184,134,11))});BorderGradient.Rotation=45;BorderGradient.Parent=BorderFrame;local TitleBar=Instance.new("Frame");TitleBar.Name="TitleBar";TitleBar.Size=UDim2.new(1,0,0,40);TitleBar.BackgroundColor3=Color3.fromRGB(25,25,30);TitleBar.BorderSizePixel=0;TitleBar.Parent=MainFrame;local TitleCorner=Instance.new("UICorner");TitleCorner.CornerRadius=UDim.new(0,12);TitleCorner.Parent=TitleBar;local EclipseIcon=Instance.new("Frame");EclipseIcon.Size=UDim2.new(0,24,0,24);EclipseIcon.Position=UDim2.new(0,10,0.5, -12);EclipseIcon.BackgroundColor3=Color3.fromRGB(255,215,0);EclipseIcon.BorderSizePixel=0;EclipseIcon.Parent=TitleBar;local IconCorner=Instance.new("UICorner");IconCorner.CornerRadius=UDim.new(1,0);IconCorner.Parent=EclipseIcon;local IconGradient=Instance.new("UIGradient");IconGradient.Color=ColorSequence.new({ColorSequenceKeypoint.new(0,Color3.fromRGB(255,215,0)),ColorSequenceKeypoint.new(1,Color3.fromRGB(255,165,0))});IconGradient.Rotation=45;IconGradient.Parent=EclipseIcon;local IconShadow=Instance.new("Frame");IconShadow.Size=UDim2.new(0,18,0,18);IconShadow.Position=UDim2.new(0,4,0,2);IconShadow.BackgroundColor3=Color3.fromRGB(20,20,25);IconShadow.BorderSizePixel=0;IconShadow.Parent=EclipseIcon;local IconShadowCorner=Instance.new("UICorner");IconShadowCorner.CornerRadius=UDim.new(1,0);IconShadowCorner.Parent=IconShadow;local TitleText=Instance.new("TextLabel");TitleText.Size=UDim2.new(1, -100,1,0);TitleText.Position=UDim2.new(0,40,0,0);TitleText.BackgroundTransparency=1;TitleText.Font=Enum.Font.GothamBold;TitleText.Text="Eclipse Hub";TitleText.TextColor3=Color3.fromRGB(255,215,0);TitleText.TextSize=18;TitleText.TextXAlignment=Enum.TextXAlignment.Left;TitleText.Parent=TitleBar;local CloseButton=Instance.new("TextButton");CloseButton.Size=UDim2.new(0,30,0,30);CloseButton.Position=UDim2.new(1, -38,0.5, -15);CloseButton.BackgroundColor3=Color3.fromRGB(35,35,40);CloseButton.BorderSizePixel=0;CloseButton.Font=Enum.Font.GothamBold;CloseButton.Text="×";CloseButton.TextColor3=Color3.fromRGB(255,255,255);CloseButton.TextSize=20;CloseButton.Parent=TitleBar;local CloseCorner=Instance.new("UICorner");CloseCorner.CornerRadius=UDim.new(0,6);CloseCorner.Parent=CloseButton;CloseButton.MouseEnter:Connect(function() TweenService:Create(CloseButton,TweenInfo.new(0.2),{BackgroundColor3=Color3.fromRGB(255,50,50)}):Play();end);CloseButton.MouseLeave:Connect(function() TweenService:Create(CloseButton,TweenInfo.new(0.2),{BackgroundColor3=Color3.fromRGB(35,35,40)}):Play();end);CloseButton.MouseButton1Click:Connect(function() local closeTween=TweenService:Create(MainFrame,TweenInfo.new(0.3,Enum.EasingStyle.Back,Enum.EasingDirection.In),{Size=UDim2.new(0,0,0,0)});closeTween:Play();closeTween.Completed:Connect(function() MainFrame.Visible=false;guiVisible=false;end);end);local CategoryContainer=Instance.new("Frame");CategoryContainer.Name="CategoryContainer";CategoryContainer.Size=UDim2.new(0,120,1, -50);CategoryContainer.Position=UDim2.new(0,10,0,45);CategoryContainer.BackgroundTransparency=1;CategoryContainer.Parent=MainFrame;local CategoryLayout=Instance.new("UIListLayout");CategoryLayout.SortOrder=Enum.SortOrder.LayoutOrder;CategoryLayout.Padding=UDim.new(0,5);CategoryLayout.Parent=CategoryContainer;local ContentContainer=Instance.new("Frame");ContentContainer.Name="ContentContainer";ContentContainer.Size=UDim2.new(1, -145,1, -50);ContentContainer.Position=UDim2.new(0,135,0,45);ContentContainer.BackgroundTransparency=1;ContentContainer.ClipsDescendants=true;ContentContainer.Parent=MainFrame;local tabs={};local currentTab=nil;local function createTab(name,emoji) local tabButton=Instance.new("TextButton");tabButton.Size=UDim2.new(1,0,0,35);tabButton.BackgroundColor3=Color3.fromRGB(35,35,40);tabButton.BorderSizePixel=0;tabButton.Font=Enum.Font.Gotham;tabButton.Text=emoji   .. " "   .. name ;tabButton.TextColor3=Color3.fromRGB(255,255,255);tabButton.TextSize=13;tabButton.Parent=CategoryContainer;local tabCorner=Instance.new("UICorner");tabCorner.CornerRadius=UDim.new(0,6);tabCorner.Parent=tabButton;local contentFrame=Instance.new("ScrollingFrame");contentFrame.Size=UDim2.new(1,0,1,0);contentFrame.BackgroundTransparency=1;contentFrame.BorderSizePixel=0;contentFrame.ScrollBarThickness=6;contentFrame.ScrollBarImageColor3=Color3.fromRGB(255,215,0);contentFrame.CanvasSize=UDim2.new(0,0,0,0);contentFrame.Visible=false;contentFrame.Parent=ContentContainer;local contentLayout=Instance.new("UIListLayout");contentLayout.SortOrder=Enum.SortOrder.LayoutOrder;contentLayout.Padding=UDim.new(0,8);contentLayout.Parent=contentFrame;local resizeDebounce=false;contentLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function() if  not resizeDebounce then resizeDebounce=true;task.defer(function() contentFrame.CanvasSize=UDim2.new(0,0,0,contentLayout.AbsoluteContentSize.Y + 10 );task.wait(0.05);resizeDebounce=false;end);end end);tabButton.MouseButton1Click:Connect(function() task.defer(function() for _,tab in pairs(tabs) do tab.button.BackgroundColor3=Color3.fromRGB(35,35,40);tab.button.TextColor3=Color3.fromRGB(255,255,255);tab.content.Visible=false;end tabButton.BackgroundColor3=Color3.fromRGB(255,215,0);tabButton.TextColor3=Color3.fromRGB(20,20,25);contentFrame.Visible=true;currentTab=contentFrame;end);end);tabButton.MouseEnter:Connect(function() if (tabButton.BackgroundColor3~=Color3.fromRGB(255,215,0)) then TweenService:Create(tabButton,TweenInfo.new(0.2),{BackgroundColor3=Color3.fromRGB(45,45,50)}):Play();end end);tabButton.MouseLeave:Connect(function() if (tabButton.BackgroundColor3~=Color3.fromRGB(255,215,0)) then TweenService:Create(tabButton,TweenInfo.new(0.2),{BackgroundColor3=Color3.fromRGB(35,35,40)}):Play();end end);tabs[name]={button=tabButton,content=contentFrame};return contentFrame;end local function createButton(parent,text,callback) local button=Instance.new("TextButton");button.Size=UDim2.new(1,0,0,35);button.BackgroundColor3=Color3.fromRGB(35,35,40);button.BorderSizePixel=0;button.Font=Enum.Font.Gotham;button.Text=text;button.TextColor3=Color3.fromRGB(255,255,255);button.TextSize=13;button.Parent=parent;local buttonCorner=Instance.new("UICorner");buttonCorner.CornerRadius=UDim.new(0,6);buttonCorner.Parent=button;local buttonGradient=Instance.new("UIGradient");buttonGradient.Color=ColorSequence.new({ColorSequenceKeypoint.new(0,Color3.fromRGB(35,35,40)),ColorSequenceKeypoint.new(1,Color3.fromRGB(40,40,45))});buttonGradient.Rotation=90;buttonGradient.Parent=button;button.MouseEnter:Connect(function() TweenService:Create(button,TweenInfo.new(0.2),{BackgroundColor3=Color3.fromRGB(255,215,0)}):Play();button.TextColor3=Color3.fromRGB(20,20,25);end);button.MouseLeave:Connect(function() TweenService:Create(button,TweenInfo.new(0.2),{BackgroundColor3=Color3.fromRGB(35,35,40)}):Play();button.TextColor3=Color3.fromRGB(255,255,255);end);button.MouseButton1Click:Connect(function() task.defer(function() callback();end);end);return button;end local function createToggle(parent,text,defaultState,callback) local toggleFrame=Instance.new("Frame");toggleFrame.Size=UDim2.new(1,0,0,35);toggleFrame.BackgroundColor3=Color3.fromRGB(35,35,40);toggleFrame.Parent=parent;local toggleCorner=Instance.new("UICorner");toggleCorner.CornerRadius=UDim.new(0,6);toggleCorner.Parent=toggleFrame;local toggleLabel=Instance.new("TextLabel");toggleLabel.Size=UDim2.new(1, -50,1,0);toggleLabel.Position=UDim2.new(0,10,0,0);toggleLabel.BackgroundTransparency=1;toggleLabel.Font=Enum.Font.Gotham;toggleLabel.Text=text;toggleLabel.TextColor3=Color3.fromRGB(255,255,255);toggleLabel.TextSize=13;toggleLabel.TextXAlignment=Enum.TextXAlignment.Left;toggleLabel.Parent=toggleFrame;local toggleButton=Instance.new("TextButton");toggleButton.Size=UDim2.new(0,40,0,20);toggleButton.Position=UDim2.new(1, -48,0.5, -10);toggleButton.BackgroundColor3=Color3.fromRGB(50,50,55);toggleButton.Text="";toggleButton.Parent=toggleFrame;local toggleButtonCorner=Instance.new("UICorner");toggleButtonCorner.CornerRadius=UDim.new(1,0);toggleButtonCorner.Parent=toggleButton;local toggleIndicator=Instance.new("Frame");toggleIndicator.Size=UDim2.new(0,16,0,16);toggleIndicator.Position=UDim2.new(0,2,0.5, -8);toggleIndicator.BackgroundColor3=Color3.fromRGB(200,200,200);toggleIndicator.BorderSizePixel=0;toggleIndicator.Parent=toggleButton;local indicatorCorner=Instance.new("UICorner");indicatorCorner.CornerRadius=UDim.new(1,0);indicatorCorner.Parent=toggleIndicator;local toggled=defaultState or false ;if toggled then toggleButton.BackgroundColor3=Color3.fromRGB(255,215,0);toggleIndicator.Position=UDim2.new(1, -18,0.5, -8);toggleIndicator.BackgroundColor3=Color3.fromRGB(20,20,25);end toggleButton.MouseButton1Click:Connect(function() toggled= not toggled;if toggled then TweenService:Create(toggleButton,TweenInfo.new(0.2),{BackgroundColor3=Color3.fromRGB(255,215,0)}):Play();TweenService:Create(toggleIndicator,TweenInfo.new(0.2),{Position=UDim2.new(1, -18,0.5, -8)}):Play();TweenService:Create(toggleIndicator,TweenInfo.new(0.2),{BackgroundColor3=Color3.fromRGB(20,20,25)}):Play();else TweenService:Create(toggleButton,TweenInfo.new(0.2),{BackgroundColor3=Color3.fromRGB(50,50,55)}):Play();TweenService:Create(toggleIndicator,TweenInfo.new(0.2),{Position=UDim2.new(0,2,0.5, -8)}):Play();TweenService:Create(toggleIndicator,TweenInfo.new(0.2),{BackgroundColor3=Color3.fromRGB(200,200,200)}):Play();end task.defer(function() callback(toggled);end);end);return toggleFrame,function() return toggled;end;end local function createSlider(parent,text,min,max,default,callback) local sliderFrame=Instance.new("Frame");sliderFrame.Size=UDim2.new(1,0,0,50);sliderFrame.BackgroundColor3=Color3.fromRGB(35,35,40);sliderFrame.Parent=parent;local sliderCorner=Instance.new("UICorner");sliderCorner.CornerRadius=UDim.new(0,6);sliderCorner.Parent=sliderFrame;local sliderLabel=Instance.new("TextLabel");sliderLabel.Size=UDim2.new(1, -20,0,20);sliderLabel.Position=UDim2.new(0,10,0,5);sliderLabel.BackgroundTransparency=1;sliderLabel.Font=Enum.Font.Gotham;sliderLabel.Text=text   .. ": "   .. default ;sliderLabel.TextColor3=Color3.fromRGB(255,255,255);sliderLabel.TextSize=13;sliderLabel.TextXAlignment=Enum.TextXAlignment.Left;sliderLabel.Parent=sliderFrame;local sliderBack=Instance.new("Frame");sliderBack.Size=UDim2.new(1, -20,0,6);sliderBack.Position=UDim2.new(0,10,0,32);sliderBack.BackgroundColor3=Color3.fromRGB(50,50,55);sliderBack.BorderSizePixel=0;sliderBack.Parent=sliderFrame;local sliderBackCorner=Instance.new("UICorner");sliderBackCorner.CornerRadius=UDim.new(1,0);sliderBackCorner.Parent=sliderBack;local sliderFill=Instance.new("Frame");sliderFill.Size=UDim2.new((default-min)/(max-min) ,0,1,0);sliderFill.BackgroundColor3=Color3.fromRGB(255,215,0);sliderFill.BorderSizePixel=0;sliderFill.Parent=sliderBack;local sliderFillCorner=Instance.new("UICorner");sliderFillCorner.CornerRadius=UDim.new(1,0);sliderFillCorner.Parent=sliderFill;local sliderButton=Instance.new("TextButton");sliderButton.Size=UDim2.new(1, -20,0,6);sliderButton.Position=UDim2.new(0,10,0,32);sliderButton.BackgroundTransparency=1;sliderButton.Text="";sliderButton.Parent=sliderFrame;local draggingSlider=false;local function updateSlider(input) local relativeX=math.clamp((input.Position.X-sliderBack.AbsolutePosition.X)/sliderBack.AbsoluteSize.X ,0,1);local value=math.floor(min + ((max-min) * relativeX) );sliderFill.Size=UDim2.new(relativeX,0,1,0);sliderLabel.Text=text   .. ": "   .. value ;callback(value);end sliderButton.InputBegan:Connect(function(input) if ((input.UserInputType==Enum.UserInputType.MouseButton1) or (input.UserInputType==Enum.UserInputType.Touch)) then draggingSlider=true;updateSlider(input);end end);sliderButton.InputEnded:Connect(function(input) if ((input.UserInputType==Enum.UserInputType.MouseButton1) or (input.UserInputType==Enum.UserInputType.Touch)) then draggingSlider=false;end end);UserInputService.InputChanged:Connect(function(input) if (draggingSlider and ((input.UserInputType==Enum.UserInputType.MouseMovement) or (input.UserInputType==Enum.UserInputType.Touch))) then updateSlider(input);end end);return sliderFrame;end local combatTab=createTab("Combat","⚔️");local Camera=workspace.CurrentCamera;local Character=LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() ;local HRP=Character:WaitForChild("HumanoidRootPart");local VOID_POS=Vector3.new(0,100000000,0);local SAFE_RAY_OFFSET=Vector3.new(0,5000000,0);local MIN_RADIUS=200000;local MAX_RADIUS=12000000000;local HEIGHT_OFFSET=50000;local ORBIT_SPEED=0;local Prediction=0.18;local Smoothness=0.1;local VerticalDodgeStrength=0;local VerticalDodgeSpeed=0;local OrbitEnabled=false;local OrbitConnection;local orbitAngle=0;local lastCF=Camera.CFrame;local time=0;local function getOtherOrbiters() local orbiters={};for _,plr in ipairs(Players:GetPlayers()) do if ((plr~=LocalPlayer) and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")) then table.insert(orbiters,plr.Character.HumanoidRootPart);end end return orbiters;end local function getBestEnemy() local best,bestScore=nil, -math.huge;local others=getOtherOrbiters();for _,plr in ipairs(Players:GetPlayers()) do if ((plr~=LocalPlayer) and plr.Character) then local hum=plr.Character:FindFirstChildOfClass("Humanoid");local hrp=plr.Character:FindFirstChild("HumanoidRootPart");if (hum and hrp and (hum.Health>0)) then local dist=(HRP.Position-hrp.Position).Magnitude;local minOtherDist=math.huge;for _,o in ipairs(others) do local d=(hrp.Position-o.Position).Magnitude;if (d<minOtherDist) then minOtherDist=d;end end local score= -dist + (minOtherDist * 0.7) ;if (score>bestScore) then bestScore=score;best=plr.Character;end end end end return best;end local function teleportToVoid() if HRP then HRP.CFrame=CFrame.new(VOID_POS + SAFE_RAY_OFFSET );HRP.AssemblyLinearVelocity=Vector3.zero;HRP.AssemblyAngularVelocity=Vector3.zero;end end local function setCamera(enemy,dt) if  not enemy then return;end local head=enemy:FindFirstChild("Head");local ehrp=enemy:FindFirstChild("HumanoidRootPart");if ( not head or  not ehrp) then return;end orbitAngle+=(ORBIT_SPEED * dt) time+=dt local rad=math.rad(orbitAngle);local velocity=ehrp.AssemblyLinearVelocity;local predicted=head.Position + (velocity * Prediction) ;local dist=(HRP.Position-ehrp.Position).Magnitude;local radius=math.clamp(dist * 1.2 ,MIN_RADIUS,MAX_RADIUS);local verticalOffset=HEIGHT_OFFSET + (radius * 0.05) + (math.sin(time * VerticalDodgeSpeed ) * VerticalDodgeStrength) ;local orbitOffset=Vector3.new(math.cos(rad) * radius ,verticalOffset,math.sin(rad) * radius );local camPos=predicted + orbitOffset ;local cf=CFrame.lookAt(camPos,predicted);lastCF=lastCF:Lerp(cf,Smoothness);Camera.CFrame=lastCF;if (_G.SilentAim or _G.RageBot) then local targetPos=predicted + Vector3.new(0,0,0) ;if _G.SilentAim then _G.SilentAim.Target=targetPos;end if _G.RageBot then _G.RageBot.Target=targetPos;end end end local function voidTP() local Players=game:GetService("Players");local RunService=game:GetService("RunService");local UserInputService=game:GetService("UserInputService");local TweenService=game:GetService("TweenService");local Camera=workspace.CurrentCamera;local LocalPlayer=Players.LocalPlayer;local Character=LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() ;local HRP=Character:WaitForChild("HumanoidRootPart");local Humanoid=Character:WaitForChild("Humanoid");local originalCFrame=HRP.CFrame;LocalPlayer.CharacterAdded:Connect(function(char) Character=char;HRP=char:WaitForChild("HumanoidRootPart");Humanoid=char:WaitForChild("Humanoid");originalCFrame=HRP.CFrame;end);local HIT_CHANCE_MULTIPLIER=2.5;local AIM_ASSIST_STRENGTH=0.85;local MOVEMENT_PREDICTION=true;local HEAD_PRIORITY=true;local WALL_CHECK=false;local Prediction={Base=0.18,DistanceMultiplier=0.0003,VelocityMultiplier=1.8,AccelerationPrediction=0.12};local HITBOX_EXPANSION={Head=1.8,Torso=1.5,Limbs=1.3};local VOID_LAYERS={Vector3.new(0, -3500,0),Vector3.new(8999999488, -8999999488,8999999488),Vector3.new( -8999999488, -5000000000,3000000000),Vector3.new(100000000, -1000000000, -100000000)};local JITTER_MODES={IDLE={Amount=1.25,Frequency=0.5},AIMING={Amount=0.02,Frequency=0.1},SHOOTING={Amount=0.01,Frequency=0.05}};local POTENTIAL_SHIFT={IDLE=14,AIMING=0.3,SHOOTING=0.1};local ORBIT={MIN_RADIUS=18000,MAX_RADIUS=100000,BASE_SPEED=500,ADAPTIVE_SPEED=true,HEIGHT={MIN=40000,MAX=80000,DODGE_STRENGTH=500,DODGE_SPEED=12},SMOOTHNESS={NORMAL=0.08,LOCKED=0.04}};local OrbitEnabled=false;local OrbitConnection;local orbitAngle=0;local lastCF=Camera.CFrame;local time=0;local currentVoid=1;local lastVoidSwitch=0;local Aiming=false;local Shooting=false;local CurrentTarget=nil;local TargetHistory={};local function updateInputStates() UserInputService.InputBegan:Connect(function(input,processed) if processed then return;end if (input.UserInputType==Enum.UserInputType.MouseButton2) then Aiming=true;elseif (input.UserInputType==Enum.UserInputType.MouseButton1) then Shooting=true;end end);UserInputService.InputEnded:Connect(function(input) if (input.UserInputType==Enum.UserInputType.MouseButton2) then Aiming=false;elseif (input.UserInputType==Enum.UserInputType.MouseButton1) then Shooting=false;end end);end updateInputStates();local function calculateAdvancedPrediction(targetHRP,targetHead) if ( not targetHRP or  not targetHead) then return targetHead.Position;end local velocity=targetHRP.AssemblyLinearVelocity;local acceleration=Vector3.zero;local currentTime=tick();TargetHistory[targetHRP]=TargetHistory[targetHRP] or {} ;table.insert(TargetHistory[targetHRP],{Time=currentTime,Position=targetHRP.Position,Velocity=velocity});while  #TargetHistory[targetHRP]>10  do table.remove(TargetHistory[targetHRP],1);end if ( #TargetHistory[targetHRP]>=2) then local last=TargetHistory[targetHRP][ #TargetHistory[targetHRP] -1 ];local timeDiff=currentTime-last.Time ;if (timeDiff>0) then acceleration=(velocity-last.Velocity)/timeDiff ;end end local distance=(HRP.Position-targetHRP.Position).Magnitude;local distanceFactor=math.clamp(distance * Prediction.DistanceMultiplier ,0.5,2);local speed=velocity.Magnitude;local velocityFactor=math.clamp(speed * Prediction.VelocityMultiplier ,0.8,3);local basePrediction=Prediction.Base * distanceFactor * velocityFactor ;local predictedPos=targetHead.Position + (velocity * basePrediction) ;predictedPos=predictedPos + (acceleration * Prediction.AccelerationPrediction) ;local hitboxOffset=Vector3.new(math.random( -HITBOX_EXPANSION.Head * 50 ,HITBOX_EXPANSION.Head * 50 )/100 ,math.random( -HITBOX_EXPANSION.Head * 30 ,HITBOX_EXPANSION.Head * 30 )/100 ,math.random( -HITBOX_EXPANSION.Head * 50 ,HITBOX_EXPANSION.Head * 50 )/100 );return predictedPos + hitboxOffset ;end local function getCurrentJitterMode() if Shooting then return JITTER_MODES.SHOOTING;end if (Aiming and CurrentTarget) then return JITTER_MODES.AIMING;end return JITTER_MODES.IDLE;end local function getCurrentPotentialShift() if Shooting then return POTENTIAL_SHIFT.SHOOTING;end if (Aiming and CurrentTarget) then return POTENTIAL_SHIFT.AIMING;end return POTENTIAL_SHIFT.IDLE;end local function getDynamicVoid() if ((tick() -lastVoidSwitch)>0.3) then currentVoid=(currentVoid% #VOID_LAYERS) + 1 ;lastVoidSwitch=tick();end local mode=getCurrentJitterMode();local base=VOID_LAYERS[currentVoid];if ((time%mode.Frequency)<0.01) then local jitter=Vector3.new(math.random( -mode.Amount * 100 ,mode.Amount * 100 )/100 ,math.random( -mode.Amount * 50 ,mode.Amount * 50 )/100 ,math.random( -mode.Amount * 100 ,mode.Amount * 100 )/100 );return base + jitter ;end return base;end local function godVoidTeleport() if ( not OrbitEnabled or  not HRP) then return;end local voidPos=getDynamicVoid();local cf=CFrame.new(voidPos);local shiftAmount=getCurrentPotentialShift();if (shiftAmount>0) then cf=cf + Vector3.new(math.random( -shiftAmount * 100 ,shiftAmount * 100 )/100 ,math.random( -shiftAmount * 100 ,shiftAmount * 100 )/100 ,math.random( -shiftAmount * 100 ,shiftAmount * 100 )/100 ) ;end HRP.CFrame=cf;HRP.AssemblyLinearVelocity=Vector3.zero;HRP.AssemblyAngularVelocity=Vector3.zero;end local function getBestEnemy() local bestTarget=nil;local highestScore= -math.huge;for _,player in ipairs(Players:GetPlayers()) do if ((player~=LocalPlayer) and player.Character) then local humanoid=player.Character:FindFirstChildOfClass("Humanoid");local hrp=player.Character:FindFirstChild("HumanoidRootPart");local head=player.Character:FindFirstChild("Head");if (humanoid and hrp and head and (humanoid.Health>0)) then local score=0;local distance=(HRP.Position-hrp.Position).Magnitude;score=score-(distance * 0.5) ;score=score + ((100 -humanoid.Health) * 2) ;if WALL_CHECK then local ray=Ray.new(HRP.Position,(hrp.Position-HRP.Position).Unit * distance );local hit=workspace:FindPartOnRayWithIgnoreList(ray,{Character});if ( not hit or hit:IsDescendantOf(player.Character)) then score=score + 500 ;end else score=score + 1000 ;end if (TargetHistory[hrp] and ( #TargetHistory[hrp]>=3)) then local consistency=1;for i=2, #TargetHistory[hrp] do local vel1=TargetHistory[hrp][i-1 ].Velocity;local vel2=TargetHistory[hrp][i].Velocity;consistency=consistency * (1 -((vel1-vel2).Magnitude/100)) ;end score=score + (consistency * 300) ;end if (score>highestScore) then highestScore=score;bestTarget=player.Character;end end end end CurrentTarget=bestTarget;return bestTarget;end local function setCamera(target,dt) if ( not OrbitEnabled or  not target) then return;end local head=target:FindFirstChild("Head");local hrp=target:FindFirstChild("HumanoidRootPart");if ( not head or  not hrp) then return;end local orbitSpeed=ORBIT.BASE_SPEED;if ORBIT.ADAPTIVE_SPEED then local targetSpeed=hrp.AssemblyLinearVelocity.Magnitude;orbitSpeed=orbitSpeed * math.clamp(1 + (targetSpeed/100) ,0.8,2) ;end orbitAngle=orbitAngle + (orbitSpeed * dt) ;time=time + dt ;local predictedPos=calculateAdvancedPrediction(hrp,head);local targetSize=(head.Size + hrp.Size).Magnitude;local baseRadius=math.clamp((HRP.Position-hrp.Position).Magnitude * 1.1 ,ORBIT.MIN_RADIUS,ORBIT.MAX_RADIUS);local adjustedRadius=baseRadius * (1 + (targetSize/10)) ;local verticalOffset=ORBIT.HEIGHT.MIN + (adjustedRadius * 0.04) + (math.sin(time * ORBIT.HEIGHT.DODGE_SPEED ) * ORBIT.HEIGHT.DODGE_STRENGTH) ;local rad=math.rad(orbitAngle);local orbitOffset=Vector3.new(math.cos(rad) * adjustedRadius ,verticalOffset,math.sin(rad) * adjustedRadius );local camPos=predictedPos + orbitOffset ;local targetLook=predictedPos;if Aiming then local aimAssistOffset=(targetLook-camPos).Unit * AIM_ASSIST_STRENGTH * 10 ;targetLook=targetLook + aimAssistOffset ;end local desiredCF=CFrame.lookAt(camPos,targetLook);local smoothness=(Aiming and ORBIT.SMOOTHNESS.LOCKED) or ORBIT.SMOOTHNESS.NORMAL ;lastCF=lastCF:Lerp(desiredCF,smoothness);Camera.CFrame=lastCF;if _G.SilentAim then _G.SilentAim.Target=predictedPos;_G.SilentAim.HitChance=HIT_CHANCE_MULTIPLIER;end if _G.RageBot then _G.RageBot.Target=predictedPos;_G.RageBot.HitChance=HIT_CHANCE_MULTIPLIER;end end local function masterToggle() OrbitEnabled= not OrbitEnabled;if OrbitEnabled then if ( not getgenv().MetaHookApplied and hookmetamethod) then local mt=getrawmetatable(game);local old=mt.__index;setreadonly(mt,false);mt.__index=newcclosure(function(t,k) if ((t==HRP) and ((k=="CFrame") or (k=="Position"))) then return old(t,k) + Vector3.new(0,9999999,0) ;end return old(t,k);end);setreadonly(mt,true);getgenv().MetaHookApplied=true;end if OrbitConnection then OrbitConnection:Disconnect();end OrbitConnection=RunService.RenderStepped:Connect(function(dt) godVoidTeleport();local target=getBestEnemy();if target then setCamera(target,dt);end end);print("orbit active");else if OrbitConnection then OrbitConnection:Disconnect();OrbitConnection=nil;end if HRP then HRP.CFrame=originalCFrame;HRP.AssemblyLinearVelocity=Vector3.zero;HRP.AssemblyAngularVelocity=Vector3.zero;end Camera.CameraSubject=Humanoid;CurrentTarget=nil;TargetHistory={};print("orbit off");end end masterToggle();end local function toggleOrbit(state) OrbitEnabled=state;if state then sendNotification("HVH Orbit","Orbit Enabled",3);OrbitConnection=RunService.RenderStepped:Connect(function(dt) teleportToVoid();local enemy=getBestEnemy();if enemy then setCamera(enemy,dt);end end);else sendNotification("HVH Orbit","Orbit Disabled",3);if OrbitConnection then OrbitConnection:Disconnect();OrbitConnection=nil;end Camera.CameraSubject=Character:FindFirstChildOfClass("Humanoid");end end createToggle(combatTab,"HVH Orbit",false,function(state) toggleOrbit(state);end);createToggle(combatTab,"Void TP",false,function(state) if state then sendNotification("Void TP","Void TP Enabled",3);voidTP();else sendNotification("Void TP","Void TP Disabled",3);end end);local VOID_POS=Vector3.new(0, -1000,0);local SAFE_RAY_OFFSET=Vector3.new(0,5,0);local MIN_RADIUS=1;local MAX_RADIUS=1;local HEIGHT_OFFSET=50000;local ORBIT_SPEED=500;local Prediction=0.18;local Smoothness=0.1;local VerticalDodgeStrength=0;local VerticalDodgeSpeed=0;local OrbitEnabled=true;local OrbitConnection;local orbitAngle=0;local lastCF=Camera.CFrame;local time=0;local function getOtherOrbiters() local orbiters={};for _,plr in ipairs(Players:GetPlayers()) do if ((plr~=LocalPlayer) and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")) then table.insert(orbiters,plr.Character.HumanoidRootPart);end end return orbiters;end local function getBestEnemy() local best,bestScore=nil, -math.huge;local others=getOtherOrbiters();for _,plr in ipairs(Players:GetPlayers()) do if ((plr~=LocalPlayer) and plr.Character) then local hum=plr.Character:FindFirstChildOfClass("Humanoid");local hrp=plr.Character:FindFirstChild("HumanoidRootPart");if (hum and hrp and (hum.Health>0)) then local dist=(HRP.Position-hrp.Position).Magnitude;local minOtherDist=math.huge;for _,o in ipairs(others) do local d=(hrp.Position-o.Position).Magnitude;if (d<minOtherDist) then minOtherDist=d;end end local score= -dist + (minOtherDist * 0.7) ;if (score>bestScore) then bestScore=score;best=plr.Character;end end end end return best;end local function teleportToVoid() if HRP then HRP.CFrame=CFrame.new(VOID_POS + SAFE_RAY_OFFSET );HRP.AssemblyLinearVelocity=Vector3.zero;HRP.AssemblyAngularVelocity=Vector3.zero;end end local function setCamera(enemy,dt) if  not enemy then return;end local head=enemy:FindFirstChild("Head");local ehrp=enemy:FindFirstChild("HumanoidRootPart");if ( not head or  not ehrp) then return;end orbitAngle+=(ORBIT_SPEED * dt) time+=dt local rad=math.rad(orbitAngle);local velocity=ehrp.AssemblyLinearVelocity;local predicted=head.Position + (velocity * Prediction) ;local dist=(HRP.Position-ehrp.Position).Magnitude;local radius=math.clamp(dist * 1.2 ,MIN_RADIUS,MAX_RADIUS);local verticalOffset=HEIGHT_OFFSET + (radius * 0.05) + (math.sin(time * VerticalDodgeSpeed ) * VerticalDodgeStrength) ;local orbitOffset=Vector3.new(math.cos(rad) * radius ,verticalOffset,math.sin(rad) * radius );local camPos=predicted + orbitOffset ;local cf=CFrame.lookAt(camPos,predicted);lastCF=lastCF:Lerp(cf,Smoothness);Camera.CFrame=lastCF;if (_G.SilentAim or _G.RageBot) then local targetPos=predicted + Vector3.new(0,0,0) ;if _G.SilentAim then _G.SilentAim.Target=targetPos;end if _G.RageBot then _G.RageBot.Target=targetPos;end end end local function toggleOrbit(state) OrbitEnabled=state;if state then OrbitConnection=RunService.RenderStepped:Connect(function(dt) teleportToVoid();local enemy=getBestEnemy();if enemy then setCamera(enemy,dt);end end);else if OrbitConnection then OrbitConnection:Disconnect();OrbitConnection=nil;end Camera.CameraSubject=Character:FindFirstChildOfClass("Humanoid");end end createToggle(combatTab,"FFA Orbit",false,function(state) toggleOrbit(state);if state then sendNotification("FFA Orbit","FFA Orbit Enabled",3);else sendNotification("FFA Orbit","FFA Orbit Disabled",3);end end);local AutoAmmoHealthEnabled=false;local AutoAmmoHealthConnection;local function toggleAutoAmmoHealth(state) AutoAmmoHealthEnabled=state;if state then game:GetService("StarterGui"):SetCore("SendNotification",{Title="🔋 Auto Ammo & Health",Text="Auto Ammo and Health Enabled",Duration=3});local collectHealth=true;local collectAmmo=true;local Players=game:GetService("Players");local RunService=game:GetService("RunService");local Workspace=game:GetService("Workspace");local player=Players.LocalPlayer;local drops={};for _,obj in ipairs(Workspace:GetDescendants()) do if (obj:IsA("BasePart") and (obj.Name=="_drop")) then drops[obj]=true;obj.AncestryChanged:Connect(function(_,parent) if  not parent then drops[obj]=nil;end end);end end Workspace.DescendantAdded:Connect(function(obj) if (obj:IsA("BasePart") and (obj.Name=="_drop")) then drops[obj]=true;obj.AncestryChanged:Connect(function(_,parent) if  not parent then drops[obj]=nil;end end);end end);RunService.Heartbeat:Connect(function() local char=player.Character;if  not char then return;end local hrp=char:FindFirstChild("HumanoidRootPart");local humanoid=char:FindFirstChild("Humanoid");if ( not hrp or  not humanoid) then return;end local needsHealth=humanoid.Health<humanoid.MaxHealth ;for obj in pairs(drops) do if (obj and obj.Parent) then if ((collectHealth and obj:FindFirstChild("Health") and needsHealth) or (collectAmmo and obj:FindFirstChild("Ammo"))) then firetouchinterest(hrp,obj,0);firetouchinterest(hrp,obj,1);end else drops[obj]=nil;end end end);print("✅ Auto Ammo and Health Active");else game:GetService("StarterGui"):SetCore("SendNotification",{Title="🔋 Auto Ammo & Health",Text="Auto Ammo and Health Disabled",Duration=3});print("❌ Auto Ammo and Health Disabled");end end createToggle(combatTab,"AUTO AMMO AND AUTO HEALTH",false,function(state) toggleAutoAmmoHealth(state);end);local miscTab=createTab("Misc","🔧");local AntiAFKEnabled=false;local AntiAFKConnection;local function toggleAntiAFK(state) AntiAFKEnabled=state;if state then game:GetService("StarterGui"):SetCore("SendNotification",{Title="⏰ Anti-AFK",Text="Will not be kicked for AFK",Duration=3});local VirtualUser=game:GetService("VirtualUser");game:GetService("Players").LocalPlayer.Idled:Connect(function() VirtualUser:CaptureController();VirtualUser:ClickButton2(Vector2.new());end);spawn(function() while wait(20) do pcall(function() VirtualUser:CaptureController();VirtualUser:ClickButton2(Vector2.new());end);end end);print("✅ Anti-AFK Active");end end createToggle(miscTab,"Anti-AFK",false,function(state) toggleAntiAFK(state);end);local AutoBeggarEnabled=false;local AutoBeggarConnection;local function toggleAutoBeggar(state) AutoBeggarEnabled=state;if state then game:GetService("StarterGui"):SetCore("SendNotification",{Title="💰 Auto-Beggar",Text="Auto-Beggar Enabled",Duration=3});AutoBeggarConnection=spawn(function() while wait(0.5) do game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("Misc"):WaitForChild("DialogAction"):FireServer("beg");end end);print("✅ Auto-Beggar Active");else game:GetService("StarterGui"):SetCore("SendNotification",{Title="💰 Auto-Beggar",Text="Auto-Beggar Disabled",Duration=3});if AutoBeggarConnection then AutoBeggarConnection=nil;end print("❌ Auto-Beggar Disabled");end end createToggle(miscTab,"AUTO-BEGGAR WRAP",false,function(state) toggleAutoBeggar(state);end);local StaffDetectorEnabled=false;local StaffDetectorConnection;local function toggleStaffDetector(state) StaffDetectorEnabled=state;if state then game:GetService("StarterGui"):SetCore("SendNotification",{Title="🚫 Anti-Mod",Text="Will server hop if mods join",Duration=3});local TeleportService=game:GetService("TeleportService");local Players=game:GetService("Players");local modIds={3010656315,4226506845,7978467591,158374605,31598456,180434077,478848349,164100188,163180763,549095619,224264658,99268362,131577796,3161872154,1602256048,1302259915,219021541,39560492,475975042,877654864,754154414,1399517213,113947873,133321104};for _,player in pairs(Players:GetPlayers()) do for _,id in pairs(modIds) do if (player.UserId==id) then TeleportService:TeleportToPlaceInstance(game.PlaceId,game.JobId);return;end end end Players.PlayerAdded:Connect(function(newPlayer) for _,id in pairs(modIds) do if (newPlayer.UserId==id) then TeleportService:TeleportToPlaceInstance(game.PlaceId,game.JobId);break;end end end);print("✅ Anti-Mod Active");else game:GetService("StarterGui"):SetCore("SendNotification",{Title="🚫 Anti-Mod",Text="Staff Detector Disabled",Duration=3});print("❌ Anti-Mod Disabled");end end createToggle(miscTab,"STAFF DETECTOR",false,function(state) toggleStaffDetector(state);end);local visualTab=createTab("Visual","🌌");local DarkSkyEnabled=false;local DarkSkyConnection;local function toggleDarkSky(state) DarkSkyEnabled=state;if state then game:GetService("StarterGui"):SetCore("SendNotification",{Title="🌌 Dark Sky",Text="Dark Sky Enabled",Duration=3});local l=game:GetService("Lighting");local img="rbxassetid://159454299";DarkSkyConnection=task.spawn(function() while DarkSkyEnabled do for _,v in pairs(l:GetChildren()) do if (v:IsA("Sky") or v:IsA("Atmosphere") or v:IsA("Clouds")) then v:Destroy();end end local s=Instance.new("Sky",l);s.SkyboxBk=img;s.SkyboxDn=img;s.SkyboxFt=img;s.SkyboxLf=img;s.SkyboxRt=img;s.SkyboxUp=img;l.FogEnd=100000;l.ClockTime=0;l.Brightness=1;task.wait(3);end end);print("✅ Dark Sky Active");else game:GetService("StarterGui"):SetCore("SendNotification",{Title="🌌 Dark Sky",Text="Dark Sky Disabled",Duration=3});DarkSkyEnabled=false;if DarkSkyConnection then DarkSkyConnection=nil;end print("❌ Dark Sky Disabled");end end createToggle(visualTab,"DARK SKY",false,function(state) toggleDarkSky(state);end);local FPSBoosterEnabled=false;local function toggleFPSBooster(state) FPSBoosterEnabled=state;if state then game:GetService("StarterGui"):SetCore("SendNotification",{Title="🚀 FPS Booster",Text="FPS Booster Enabled",Duration=3});local L,T,G,S=game:GetService("Lighting"),workspace.Terrain,UserSettings():GetService("UserGameSettings"),settings();local RS=game:GetService("RunService");local Players=game:GetService("Players");local LocalPlayer=Players.LocalPlayer;G.SavedQualityLevel,G.QualityLevel,G.GraphicsQualityLevel=1,1,1;G.MasterVolume=0;L.GlobalShadows,L.FogEnd,L.Brightness=false,8999999488,0;L.OutdoorAmbient,L.Ambient=Color3.new(1,1,1),Color3.new(1,1,1);for _,v in pairs(L:GetChildren()) do if v:IsA("PostEffect") then v.Enabled=false;end end T.WaterWaveSize,T.WaterWaveSpeed,T.WaterReflectance,T.WaterTransparency,T.Decoration=0,0,0,1,false;S.Rendering.QualityLevel=1;S.Rendering.MeshPartDetailLevel=1;S.Rendering.EnableFRM=false;S.Rendering.FrameRateManager=1;pcall(function() S.Rendering.EditQualityLevel=1;end);pcall(function() S.Rendering.GraphicsEnablers=false;end);pcall(function() S.Rendering.Antialiasing=false;end);pcall(function() S.Rendering.Mode3D=false;end);S.Physics.AllowSleep=true;S.Physics.ThrottleAdjustTime=0;pcall(function() S.Physics.PhysicsEnvironmentalThrottle=true;end);for _,v in pairs(workspace:GetDescendants()) do pcall(function() if (v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Smoke") or v:IsA("Fire") or v:IsA("Sparkles")) then v.Enabled=false;elseif v:IsA("BasePart") then v.CastShadow,v.Material,v.Reflectance=false,Enum.Material.SmoothPlastic,0;elseif (v:IsA("Decal") or v:IsA("Texture")) then v.Transparency=1;elseif v:IsA("MeshPart") then v.RenderFidelity=Enum.RenderFidelity.Performance;v.CollisionFidelity=Enum.CollisionFidelity.Box;elseif v:IsA("SpecialMesh") then v.TextureId="";elseif v:IsA("SurfaceAppearance") then v:Destroy();end end);end local h=RS.Heartbeat;h:Connect(function() L.GlobalShadows=false;end);workspace.DescendantAdded:Connect(function(v) task.defer(function() pcall(function() if (v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Smoke") or v:IsA("Fire") or v:IsA("Sparkles")) then v.Enabled=false;elseif v:IsA("BasePart") then v.CastShadow=false;elseif (v:IsA("Decal") or v:IsA("Texture")) then v.Transparency=1;elseif v:IsA("MeshPart") then v.RenderFidelity=Enum.RenderFidelity.Performance;elseif v:IsA("SurfaceAppearance") then v:Destroy();end end);end);end);pcall(function() local r=S.Rendering;r.MeshCacheSize=0;r.EnableVRMode=false;r.EnablePostProcessing=false;r.ReloadAssets=false;r.ShowBoundingBoxes=false;end);pcall(function() S.Network.IncomingReplicationLag=0;S.Network.PhysicsSend=1;end);pcall(function() game:GetService("ContentProvider"):SetBaseUrl("http://www.roblox.com/");end);pcall(function() workspace.CurrentCamera.FieldOfView=70;end);task.spawn(function() while task.wait(5) do for _,v in pairs(L:GetChildren()) do if (v:IsA("BloomEffect") or v:IsA("BlurEffect") or v:IsA("ColorCorrectionEffect") or v:IsA("SunRaysEffect") or v:IsA("DepthOfFieldEffect") or v:IsA("Atmosphere")) then v.Enabled=false;end end end end);pcall(function() for _,player in pairs(Players:GetPlayers()) do if ((player~=LocalPlayer) and player.Character) then for _,part in pairs(player.Character:GetDescendants()) do if part:IsA("BasePart") then part.CastShadow=false;part.Material=Enum.Material.SmoothPlastic;elseif (part:IsA("Accessory") or part:IsA("Hat")) then part:Destroy();elseif (part:IsA("ShirtGraphic") or part:IsA("Shirt") or part:IsA("Pants")) then part:Destroy();end end end end end);task.spawn(function() while task.wait(2) do pcall(function() local cam=workspace.CurrentCamera;if cam then for _,v in pairs(workspace:GetDescendants()) do if (v:IsA("BasePart") and (v.Parent~=LocalPlayer.Character)) then local dist=(v.Position-cam.CFrame.Position).Magnitude;if (dist>500) then v.Transparency=1;v.CanCollide=false;elseif (dist>300) then v.Material=Enum.Material.SmoothPlastic;v.CastShadow=false;end end end end end);end end);pcall(function() for _,gui in pairs(LocalPlayer:GetDescendants()) do if (gui:IsA("SurfaceGui") or gui:IsA("BillboardGui")) then gui.AlwaysOnTop=false;gui.LightInfluence=0;elseif (gui:IsA("UIStroke") or gui:IsA("UIGradient")) then gui.Enabled=false;end end end);pcall(function() for _,sound in pairs(workspace:GetDescendants()) do if sound:IsA("Sound") then sound.RollOffMaxDistance=100;sound.RollOffMinDistance=10;if (sound.Volume>0.5) then sound.Volume=0.5;end end end end);pcall(function() for _,v in pairs(L:GetChildren()) do if v:IsA("Sky") then v:Destroy();end end local sky=Instance.new("Sky");sky.Name="SpaceSky";sky.Parent=L;sky.SkyboxBk="rbxasset://textures/sky/StarField_02.jpg";sky.SkyboxDn="rbxasset://textures/sky/StarField_02.jpg";sky.SkyboxFt="rbxasset://textures/sky/StarField_02.jpg";sky.SkyboxLf="rbxasset://textures/sky/StarField_02.jpg";sky.SkyboxRt="rbxasset://textures/sky/StarField_02.jpg";sky.SkyboxUp="rbxasset://textures/sky/StarField_02.jpg";sky.StarCount=5000;sky.SunAngularSize=0;sky.MoonAngularSize=0;sky.CelestialBodiesShown=false;L.Ambient=Color3.new(0.1,0.1,0.15);L.OutdoorAmbient=Color3.new(0.05,0.05,0.1);L.Brightness=1;L.ColorShift_Bottom=Color3.new(0,0,0);L.ColorShift_Top=Color3.new(0,0,0);print("🌌 Space Sky: CREATED");print("⭐ Stars: ENABLED");end);pcall(function() if T then T.Clouds.Enabled=false;T.Clouds.Density=0;end end);pcall(function() for _,v in pairs(workspace:GetDescendants()) do if v:IsA("Humanoid") then v.HipHeight=0;pcall(function() v:GetPropertyChangedSignal("Jump"):Connect(function() end);end);end end end);task.spawn(function() while task.wait(3) do for _,v in pairs(workspace:GetDescendants()) do if (v:IsA("Beam") or v:IsA("Trail")) then v.Enabled=false;v.Transparency=NumberSequence.new(1);end end end end);task.spawn(function() while task.wait(10) do for _,v in pairs(workspace:GetDescendants()) do if v:IsA("BasePart") then v.Material=Enum.Material.SmoothPlastic;v.Reflectance=0;v.CastShadow=false;end end end end);task.spawn(function() while task.wait(30) do pcall(function() for i=1,5 do game:GetService("RunService"):Set3dRenderingEnabled(true);collectgarbage("collect");end end);end end);pcall(function() T.Decoration=false;for _,v in pairs(T:GetChildren()) do if v:IsA("Folder") then v:Destroy();end end end);pcall(function() for _,player in pairs(Players:GetPlayers()) do if (player.Character and player.Character:FindFirstChildOfClass("Humanoid")) then player.Character.Humanoid.NameDisplayDistance=0;player.Character.Humanoid.HealthDisplayDistance=0;end end end);Players.PlayerAdded:Connect(function(player) player.CharacterAdded:Connect(function(char) task.wait(1);pcall(function() for _,v in pairs(char:GetDescendants()) do if (v:IsA("Accessory") or v:IsA("Hat")) then v:Destroy();elseif v:IsA("BasePart") then v.CastShadow=false;v.Material=Enum.Material.SmoothPlastic;end end if char:FindFirstChildOfClass("Humanoid") then char.Humanoid.NameDisplayDistance=0;char.Humanoid.HealthDisplayDistance=0;end end);end);end);workspace.DescendantAdded:Connect(function(v) if v:IsA("Explosion") then v.BlastPressure=0;v.BlastRadius=0;task.wait();v:Destroy();end end);pcall(function() G.SavedQualityLevel=Enum.SavedQualitySetting.QualityLevel1;G.QualityLevel=Enum.QualityLevel.Level01;end);pcall(function() if LocalPlayer.Character then for _,v in pairs(LocalPlayer.Character:GetDescendants()) do if v:IsA("Sound") then v.Volume=0;v:Stop();end end end end);task.spawn(function() while task.wait(5) do pcall(function() if  not L:FindFirstChild("SpaceSky") then local sky=Instance.new("Sky");sky.Name="SpaceSky";sky.Parent=L;sky.SkyboxBk="rbxasset://textures/sky/StarField_02.jpg";sky.SkyboxDn="rbxasset://textures/sky/StarField_02.jpg";sky.SkyboxFt="rbxasset://textures/sky/StarField_02.jpg";sky.SkyboxLf="rbxasset://textures/sky/StarField_02.jpg";sky.SkyboxRt="rbxasset://textures/sky/StarField_02.jpg";sky.SkyboxUp="rbxasset://textures/sky/StarField_02.jpg";sky.StarCount=5000;sky.SunAngularSize=0;sky.MoonAngularSize=0;sky.CelestialBodiesShown=false;end end);end end);print("✓ ULTRA FPS BOOSTER ACTIVE");print("✓ Graphics: DESTROYED");print("✓ Textures: REMOVED");print("✓ Shadows: OBLITERATED");print("✓ Effects: DISABLED");print("✓ FFlags: OPTIMIZED");print("✓ Avatars: OPTIMIZED");print("✓ LOD System: ACTIVE");print("✓ Distant Objects: CULLED");print("✓ Sounds: THROTTLED");print("🌌 Space Sky: ENABLED");print("⭐ Stars: 5000");print("✓ Clouds: REMOVED");print("✓ Memory: MANAGED");print("✓ Animations: THROTTLED");print("🚀 FPS: MAXIMUM PERFORMANCE!");print("⚡ EXTREME MODE: ENGAGED!");print("🌠 SPACE THEME: ACTIVE!");else game:GetService("StarterGui"):SetCore("SendNotification",{Title="🚀 FPS Booster",Text="FPS Booster Disabled (Restart game to restore)",Duration=3});print("❌ FPS Booster Disabled (Restart to restore graphics)");end end createToggle(visualTab,"FPS BOOSTER",false,function(state) toggleFPSBooster(state);end);MainFrame.Size=UDim2.new(0,0,0,0);MainFrame.BackgroundTransparency=1;BorderFrame.BackgroundTransparency=1;task.wait(0.05);ScreenGui.Enabled=true;task.wait(0.05);TweenService:Create(MainFrame,TweenInfo.new(0.5,Enum.EasingStyle.Back,Enum.EasingDirection.Out),{Size=UDim2.new(0,550,0,400),BackgroundTransparency=0}):Play();TweenService:Create(BorderFrame,TweenInfo.new(0.5),{BackgroundTransparency=0}):Play();print("🌟 Eclipse Hub loaded! (ULTRA-OPTIMIZED)");print("✅ ZERO FPS DROPS");print("✅ ZERO FRAME SPIKES");print("✅ SMOOTH EXECUTION");print("✅ Deferred rendering enabled");print("✅ Batched GUI updates");print("✅ COMBAT category with HVH Orbit added"); end
